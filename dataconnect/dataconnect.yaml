specVersion: "v1"
serviceId: "EMob"
location: "europe-west1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "emob-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
